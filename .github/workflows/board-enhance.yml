name: Add UZDphoto board enhancement
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Attach board enhancement
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          marker='<!-- BOARD_ENHANCEMENT -->'
          if marker not in s:
              s=s.replace('</body>', marker+'<script src="board-enhance.js"></script></body>')
              p.write_text(s,encoding='utf-8')
          PY
      - name: Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html board-enhance.js
          git diff --cached --quiet || git commit -m 'Improve station board with schedule management'
          git push
